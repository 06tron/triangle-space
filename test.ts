const getMessage = () => "Hello World!";

/**
 * @param x - The value to increment.
 * @returns A number greater than 'x'.
 */
function incr(x: number) {
    return x + 1;
}

incr(4);
console.log(getMessage());
